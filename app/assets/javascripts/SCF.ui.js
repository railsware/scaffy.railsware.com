SCF = {};

$(document).ready(function() {
    // Init custom selects
    $(".select").chosen();

    // Init slideshow
    SCF.Slideshow.init();

    // Init appreciate
    SCF.Appreciate.init();
});

